{
  "name": "@second-memory/worker-embeddings",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "uv run python -m worker_embeddings.main",
    "build": "uv sync",
    "lint": "uv run python -m compileall src/worker_embeddings",
    "test": "uv run pytest"
  }
}
