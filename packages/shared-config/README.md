# @second-memory/shared-config

Shared configuration presets for linting, TypeScript, and build tooling.

Planned exports:

- eslint.base.cjs
- tsconfig.base.json extensions
- service and app-specific config variants
